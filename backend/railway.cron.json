{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "python -m jobs.daily_snapshot",
    "restartPolicyType": "NEVER"
  }
}
